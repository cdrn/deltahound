# Migrated

Deltahound now lives in [backdraft](https://github.com/cdrn/backdraft) as the
`delta` module (`src/delta/`), running as its own container so the dataset
stays continuous while the scanner/executor iterates.

This repo is frozen at the point of migration (2026-06-10). The quote history
collected here was carried over into the delta database.
